from random import randrange

nums = []

def checkDuplicate(num_list,taget):
    result = false

    for x in num_list:
        if x == taget:
            result = True
            break

    return result

checkDuplicate=false
    for x in nums:
        if num == x:
            checkResult


# 루프를 nums가 6개가 되는 동안
while len(nums) < 6:

    # 1부터 45사이의 숫자를 뽑는다.
    num = randrage(1, 46)

    # num을 확인하기
    if num == nums
        nums.append((num))

    nums.append(num)

print(nums)


# 뽑은 값이 있다면 다시 뽑는다.